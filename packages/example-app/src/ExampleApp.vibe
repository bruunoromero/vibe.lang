module ExampleApp exposing (..)

import Vibe exposing (..)
import Vibe.Ref as Ref
import Vibe.Promise as Promise exposing (Promise)
import Vibe.Task as Task

@external "./ExampleApp.ffi.js" "readFile"
readFile : String -> Promise String

type Person =
    { name : String
    , age : Int
    }

main =
  if True then
    "It's TRUE"
  else
    "It's FALSE"
-- main = "String"