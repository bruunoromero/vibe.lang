{
  "name": "@vibe/example-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "bun run ../cli/index.ts run --file src/main.lang",
    "build": "bun run ../cli/index.ts build --force",
    "start": "node dist/main.js",
    "fmt": "bun run ../cli/index.ts fmt"
  },
  "dependencies": {
    "@vibe/prelude": "workspace:*"
  }
}
