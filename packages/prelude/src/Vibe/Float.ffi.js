export const floatAdd = (a) => (b) => a + b;

export const floatSub = (a) => (b) => a - b;

export const floatMul = (a) => (b) => a * b;

export const floatDiv = (a) => (b) => a / b;

export const numToString = (n) => n.toString();

export const numEq = (a) => (b) => a === b;

export const numLt = (a) => (b) => a < b;

export const numGt = (a) => (b) => a > b;
